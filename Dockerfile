# Используем официальный образ Golang в качестве базового образа
FROM golang:latest

# Устанавливаем рабочую директорю внутри контейнера
WORKDIR /billing

# Копируем все файлы из текущей директории внутрь контейнера
COPY . .

# Запускаем команду для сборки приложения
# RUN go build -o billing .

# Указываем порт, который будет использоваться для взаимодействия с контейнером
EXPOSE 8080

# Запускаем приложение при стте контейнера
CMD ["./billing"]
